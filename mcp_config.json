{
  "mcpServers": {
    "mysql": {
      "command": "npx",
      "args": [
        "-y",
        "@toolbox-sdk/server@1.8.0",
        "--prebuilt",
        "mysql",
        "--stdio"
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
